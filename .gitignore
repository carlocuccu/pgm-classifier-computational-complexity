__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/
.pytest_cache/
.venv/
venv/

# Notebook output (regenerated by the notebook)
results_table7.csv

# The self-test output is not a reported result.
results_benchmark/selftest.csv

# The rest of results_benchmark/ is NOT ignored: it holds the run reported in
# the paper. Re-running the harness overwrites it in place -- check `git status` before
# committing, and keep the deposited run unless you mean to replace it.

# The data is not carried in the repository: `python scripts/fetch_datasets.py`
# downloads it and rebuilds it, byte for byte, from its public sources.
datasets/
