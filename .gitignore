__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/
.pytest_cache/
.venv/
venv/

# Notebook output (regenerated by the notebook)
results_table7.csv

# results_benchmark/ is NOT ignored: it holds the run reported in the paper.
# Re-running the harness overwrites it in place -- check `git status` before
# committing, and keep the deposited run unless you mean to replace it.

# Not redistributed: fetched by datasets/download_skin_segmentation.py
datasets/Skin_NonSkin.txt
